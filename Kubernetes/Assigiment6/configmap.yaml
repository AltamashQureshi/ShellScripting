apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_ENV: production
  APP_PORT: '8080'
  LOG_LEVEL: info
  database.conf: |
    host=postgres.default.svc.cluster.local
    port=5432
    name=myapp
    pool_size=10